import {
  log,
  part1,
  part2,
  printTime,
  readLines,
  setDebug,
  startTimer,
} from "./aoc.ts";

startTimer();
const example = true;
setDebug(example);
const lines = readLines(example);
log(lines);

// part1
part1("TODO");
printTime();

// part2
part2("TODO");
printTime();
